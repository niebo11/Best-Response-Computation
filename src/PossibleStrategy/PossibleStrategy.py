def Utility(G, C, T, alpha, target_size):
    result = alpha * len(T)
    target_objectives = [item for item in C if G.nodes[item]['target']]



def PossibleStrategy(G, alpha, target_size):
    return
